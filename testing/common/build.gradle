apply plugin: "org.jetbrains.intellij"

intellij {
    version = intellijSdkVersion
    plugins = ['java']
}

dependencies {
    implementation project(':graph-database-support-plugin')
    annotationProcessor("org.apache.tinkerpop:gremlin-core:$tinkerpopVersion")
}
